const Bank = require('../../database/models/bank')



const deleteBankJobs = async (req, res, next) => {
    // try {
        // const data = await Bank.deleteMany({
            // title: 'Bnak Asia job circular'
        // })
// 
        // res.status(200).send(data)
    // } catch (e) {
        // res.status(404).send(e.name + ' ' + e.message)
    // }
    res.send('Done')

}





module.exports = deleteBankJobs